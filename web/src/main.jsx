import React,{useEffect,useState} from 'react';
import {createRoot} from 'react-dom/client';
import './style.css';
const API=import.meta.env.VITE_API_URL||'http://localhost:3001';
const empty={codigo:'',nome_completo:'',cpf_cnpj:'',nascimento:'',naturalidade:'',filiacao_pai:'',filiacao_mae:'',estado_civil:'',rg:'',rg_orgao:'',tipo_cliente:'Pessoa Física',telefone:'',whatsapp:'',email:'',cep:'',endereco:'',numero:'',complemento:'',bairro:'',cidade:'',estado:'',origem_cadastro:'',responsavel:'',status:'ativo',categoria_cac:'',cr_numero:'',cr_emissao:'',cr_validade:'',cr_situacao:'',cr_orgao:'',observacoes:''};
function F({label,name,type='text',required=false,form,setForm}){return <label><span>{label}{required?' *':''}</span><input type={type} value={form[name]||''} required={required} onChange={e=>setForm(prev=>({...prev,[name]:e.target.value}))}/></label>}

function getDateParts(value){
  if(value===null||value===undefined||value==='')return null;
  const raw=String(value).trim().split('T')[0];
  const iso=raw.split('-');
  if(iso.length===3&&iso[0].length===4&&iso[1].length===2&&iso[2].length===2){
    return {y:iso[0],m:iso[1],d:iso[2]};
  }
  const br=raw.split('/');
  if(br.length===3&&br[0].length===2&&br[1].length===2&&br[2].length===4){
    return {y:br[2],m:br[1],d:br[0]};
  }
  return null;
}
function formatDateBR(value){
  const p=getDateParts(value);
  return p ? p.d+'/'+p.m+'/'+p.y : '';
}
function formatDateInput(value){
  const p=getDateParts(value);
  return p ? p.y+'-'+p.m+'-'+p.d : '';
}
function getNascimento(detail){
  return formatDeclaracaoDate(detail?.nascimento ?? detail?.data_nascimento ?? detail?.dataNascimento ?? detail?.data_de_nascimento);
}
function formatDeclaracaoDate(value){
  const p=getDateParts(value);
  return p ? p.d+'/'+p.m+'/'+p.y : '';
}
function dataEmissaoAtual(){const d=new Date();return String(d.getDate()).padStart(2,'0')+' de '+['janeiro','fevereiro','março','abril','maio','junho','julho','agosto','setembro','outubro','novembro','dezembro'][d.getMonth()]+' de '+d.getFullYear()}
function dataValidadeAtual(){const d=new Date();d.setMonth(d.getMonth()+3);return String(d.getDate()).padStart(2,'0')+'/'+String(d.getMonth()+1).padStart(2,'0')+'/'+d.getFullYear()}
function nomeCliente(d){return String(d?.nome_completo||d?.razao_social||d?.nome||'').toUpperCase()}
function enderecoCliente(d){return [d?.endereco,d?.numero,d?.complemento,d?.bairro].filter(Boolean).join(', ')}
function cidadeEstadoCliente(d){return [d?.cidade,d?.estado].filter(Boolean).join(' - ')}
async function imprimirDeclaracao(declaracaoAtual){
  const paper=document.querySelector('.printDeclaration');
  if(!paper){alert('Não foi possível preparar a declaração em PDF.');return}
  try{
    const {jsPDF}=await import('jspdf');
    const doc=new jsPDF({orientation:'portrait',unit:'mm',format:'a4',compress:true});
    const clone=paper.cloneNode(true);
    clone.style.width='210mm';
    clone.style.height='297mm';
    clone.style.minHeight='297mm';
    clone.style.margin='0';
    clone.style.padding='24mm 22mm';
    clone.style.boxSizing='border-box';
    clone.style.position='fixed';
    clone.style.left='-10000px';
    clone.style.top='0';
    clone.style.background='#fff';
    clone.style.color='#000';
    document.body.appendChild(clone);
    await doc.html(clone,{
      x:0,y:0,width:210,windowWidth:794,
      margin:[0,0,0,0],
      autoPaging:'text',
      html2canvas:{scale:2,useCORS:true,backgroundColor:'#ffffff',logging:false},
      callback:(pdf)=>{
        document.body.removeChild(clone);
        const dadosPdf=declaracaoAtual||{};
        const nome=nomeCliente(dadosPdf.detail)||'cliente';
        const tipo=dadosPdf.tipo||'declaracao';
        pdf.save(tipo+'-'+nome.replace(/[^a-z0-9]+/gi,'-').replace(/^-|-$/g,'').toLowerCase()+'.pdf');
      }
    });
  }catch(e){
    const cloneAtual=document.querySelector('.printDeclaration[style*="-10000px"]');
    if(cloneAtual)cloneAtual.remove();
    const paper=document.querySelector('.printDeclaration');
    if(paper)alert('Não foi possível gerar o PDF automaticamente. Erro: '+(e?.message||e));
  }
}
function App(){const[token,setToken]=useState(localStorage.getItem('token'));const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[dash,setDash]=useState(null);const[clientes,setClientes]=useState([]);const[show,setShow]=useState(false);const[detail,setDetail]=useState(null);const[form,setForm]=useState(empty);const[editing,setEditing]=useState(false);const[erro,setErro]=useState('');const[ok,setOk]=useState('');const[usuario,setUsuario]=useState(null);const[usuarios,setUsuarios]=useState([]);const[showUsuarios,setShowUsuarios]=useState(false);const[usuarioForm,setUsuarioForm]=useState({email:'',senha:'',role:'operacional'});const[senhaForm,setSenhaForm]=useState({atual:'',nova:''});const[senhaUsuarioId,setSenhaUsuarioId]=useState(null);
const [declaracao,setDeclaracao]=useState(null);
const login=async e=>{e.preventDefault();setErro('');try{const r=await fetch(API+'/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw new Error(d.error);localStorage.setItem('token',d.token);setToken(d.token)}catch(x){setErro(x.message)}};
const loadUsuarios=async()=>{try{const r=await fetch(API+'/api/usuarios',{headers:{Authorization:'Bearer '+token}});const d=await r.json();if(r.ok)setUsuarios(d)}catch(e){setErro('Não foi possível carregar os usuários')}};
const load=async()=>{
  try{
    const h={Authorization:'Bearer '+token};
    const [a,b,m]=await Promise.all([fetch(API+'/api/dashboard',{headers:h}),fetch(API+'/api/clientes',{headers:h}),fetch(API+'/api/auth/me',{headers:h})]);
    if(a.status===401||b.status===401){localStorage.removeItem('token');setToken(null);return}
    const da=await a.json(); const db=await b.json(); const dm=await m.json();
    if(!a.ok)throw new Error(da.error||'Erro ao carregar dashboard');
    if(!b.ok)throw new Error(db.error||'Erro ao carregar clientes');
    setDash(da);setClientes(db);if(m.ok){setUsuario(dm.user);if(dm.user.role==='administrador')loadUsuarios()}
  }catch(e){setErro(e.message||'Não foi possível carregar os dados');}
};
useEffect(()=>{if(token)load()},[token]);
const save=async e=>{
  e.preventDefault();setErro('');setOk('');
  try{
    const r=await fetch(API+(editing?'/api/clientes/'+form.id:'/api/clientes'),{method:editing?'PUT':'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify(form)});
    const d=await r.json();
    if(!r.ok){setErro(d.error||'Não foi possível salvar');return}
    setOk(editing?'Cliente atualizado com sucesso.':'Cliente cadastrado com sucesso.');
    setForm({...empty});setEditing(false);await load();setTimeout(()=>setShow(false),700);
  }catch(e){setErro('Não foi possível salvar o cliente. Verifique a conexão com o sistema.');}
};
if(!token)return <main className="login"><section><h1>CAC GESTÃO</h1><p>Controle e gestão de clientes</p><form onSubmit={login}><input placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Senha" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button>Entrar</button>{erro&&<small>{erro}</small>}</form></section></main>;
return <main><header><div className="brandBlock"><div className="brandMark">◎</div><div><h1>CAC GESTÃO</h1><p>Controle e gestão de clientes</p></div></div><div className="headerMeta"><span className="rangeBadge">● AMBIENTE CAC</span><button className="secondary" onClick={()=>{localStorage.removeItem('token');setToken(null)}}>Sair</button></div></header>
<div className="toolbar"><div><span className="sectionKicker">CENTRAL DE CONTROLE</span><h2>Dashboard</h2></div><div className="toolbarActions">{usuario?.role==='administrador'&&<button className="secondary" onClick={()=>{setErro('');setOk('');setShowUsuarios(true);loadUsuarios()}}>⚙ Usuários</button>}<button onClick={()=>{setErro('');setOk('');setEditing(false);setForm(empty);setShow(true)}}>+ Novo cliente</button></div></div>
<div className="cards">{dash&&Object.entries(dash).map(([k,v])=><article key={k}><b>{v}</b><span>{k}</span></article>)}</div>
<section className="panel"><div className="panelhead"><h2>Clientes cadastrados</h2><span>{clientes.length} registro(s)</span></div>{clientes.map(c=><div className="row" key={c.id}><div><button className="nameButton" onClick={async()=>{setErro('');try{const r=await fetch(API+'/api/clientes/'+c.id,{headers:{Authorization:'Bearer '+token}});const d=await r.json();if(r.ok)setDetail(d);else setErro(d.error||'Erro ao consultar cliente')}catch(e){setErro('Erro de comunicação ao consultar cliente')}}}>{c.nome_completo||c.nome||c.razao_social}</button><small>{c.cpf_cnpj||'Sem CPF/CNPJ'} · {c.cidade||'Cidade não informada'}</small></div><span>{c.categoria_cac||c.tipo_cliente||'—'}</span><span className={c.status==='ativo'?'active':''}>{c.status}</span></div>)}{!clientes.length&&<p>Nenhum cliente cadastrado.</p>}</section>
{detail&&<div className="overlay"><section className="modal detailModal"><div className="modalhead"><div><h2>{detail.nome_completo||detail.nome||detail.razao_social}</h2><p>Perfil do cliente</p></div><button type="button" className="close" onClick={()=>setDetail(null)}>×</button></div><div className="detailGrid">{[['Código',detail.codigo],['CPF/CNPJ',detail.cpf_cnpj],['Nascimento',formatDeclaracaoDate(detail.nascimento||detail.data_nascimento||detail.dataNascimento||detail.data_de_nascimento)],['Naturalidade',detail.naturalidade],['Filiação - Pai',detail.filiacao_pai],['Filiação - Mãe',detail.filiacao_mae],['Estado civil',detail.estado_civil],['RG',detail.rg],['Órgão RG',detail.rg_orgao],['Tipo',detail.tipo_cliente],['Status',detail.status],['Telefone',detail.telefone],['WhatsApp',detail.whatsapp],['E-mail',detail.email],['Endereço',[detail.endereco,detail.numero,detail.complemento].filter(Boolean).join(', ')],['Cidade',[detail.cidade,detail.estado].filter(Boolean).join(' - ')],['Categoria CAC',detail.categoria_cac],['Número do CR',detail.cr_numero],['Validade do CR',detail.cr_validade],['Situação do CR',detail.cr_situacao],['Órgão emissor',detail.cr_orgao],['Responsável',detail.responsavel],['Origem do cadastro',detail.origem_cadastro]].map(([l,v])=><div className="detailItem" key={l}><span>{l}</span><b>{v||'—'}</b></div>)}</div><div className="detailObs"><span>Observações</span><p>{detail.observacoes||'—'}</p></div><div className="actions"><button type="button" className="secondary" onClick={()=>setDetail(null)}>Fechar</button><button type="button" onClick={()=>setDeclaracao({tipo:'menu',detail:{...detail}})}>Gerar declaração</button><button type="button" onClick={()=>{setForm({...empty,...detail,nascimento:formatDateInput(detail.nascimento),cr_emissao:formatDateInput(detail.cr_emissao),cr_validade:formatDateInput(detail.cr_validade)});setEditing(true);setDetail(null);setShow(true)}}>Editar cliente</button></div>{declaracao&&declaracao.tipo==='menu'&&detail&&<div className="declarationInlineMenu"><div className="declarationInlineHeader"><strong>Selecione a declaração</strong><button type="button" className="close" onClick={()=>setDeclaracao(null)}>×</button></div><button type="button" className="declarationOption" onClick={()=>setDeclaracao({tipo:'inexistencia',detail:declaracao.detail})}><strong>Declaração de Inexistência de Inquéritos Policiais ou Processos Criminais</strong><span>Utiliza os dados pessoais, filiação e endereço do cliente.</span></button><button type="button" className="declarationOption" onClick={()=>setDeclaracao({tipo:'titular-form',detail:declaracao.detail,titular:{nome:'',rg:'',cpf:''}})}><strong>Declaração do Titular do Endereço</strong><span>Informe os dados do titular do imóvel.</span></button><button type="button" className="declarationOption" onClick={()=>setDeclaracao({tipo:'dsa',detail:declaracao.detail})}><strong>Declaração de Segurança do Acervo (DSA)</strong><span>Utiliza os dados do cliente e do endereço cadastrado.</span></button></div>}</section></div>}
{declaracao&&declaracao.tipo!=='menu'&&<div className="overlay declarationOverlay"><section className="modal declarationChooser">
{declaracao.tipo==='titular-form'&&<><div className="modalhead"><div><h2>Declaração do Titular do Endereço</h2><p>Informe os dados do titular do imóvel</p></div><button type="button" className="close" onClick={()=>setDeclaracao(null)}>×</button></div><div className="grid declarationFormGrid"><label><span>Nome do titular *</span><input value={declaracao.titular?.nome||''} onChange={e=>setDeclaracao(prev=>({...prev,titular:{...prev.titular,nome:e.target.value}}))}/></label><label><span>RG do titular</span><input value={declaracao.titular?.rg||''} onChange={e=>setDeclaracao(prev=>({...prev,titular:{...prev.titular,rg:e.target.value}}))}/></label><label><span>CPF do titular *</span><input value={declaracao.titular?.cpf||''} onChange={e=>setDeclaracao(prev=>({...prev,titular:{...prev.titular,cpf:e.target.value}}))}/></label></div><div className="actions"><button type="button" className="secondary" onClick={()=>setDeclaracao({tipo:'menu',detail:declaracao.detail})}>Voltar</button><button type="button" onClick={()=>{if(!declaracao.titular?.nome||!declaracao.titular?.cpf){alert('Informe o nome e o CPF do titular do endereço.');return}setDeclaracao(prev=>({...prev,tipo:'titular'}))}}>Continuar</button></div></>}
{(declaracao.tipo==='inexistencia'||declaracao.tipo==='titular'||declaracao.tipo==='dsa')&&<><div className="modalhead noPrint"><div><h2>Declaração</h2><p>Pré-visualização para impressão</p></div><button type="button" className="close" onClick={()=>setDeclaracao(null)}>×</button></div><div className="declarationPaper printDeclaration">{declaracao.tipo==='inexistencia'&&<><h1>DECLARAÇÃO DE INEXISTÊNCIA DE INQUÉRITOS POLICIAIS OU<br/>PROCESSOS CRIMINAIS</h1><p>Eu, <strong>{nomeCliente(declaracao.detail)}</strong>, BRASILEIRO, natural de <strong>{String(declaracao.detail?.naturalidade||'').toUpperCase()}</strong>, nascido em <strong>{getNascimento(declaracao.detail)||'NÃO INFORMADO'}</strong>, filho(a) de <strong>{String(declaracao.detail?.filiacao_pai||'').toUpperCase()}</strong> e <strong>{String(declaracao.detail?.filiacao_mae||'').toUpperCase()}</strong>, <strong>{String(declaracao.detail?.estado_civil||'').toUpperCase()}</strong> residência no(a), <strong>{[declaracao.detail?.endereco,declaracao.detail?.numero,declaracao.detail?.complemento,declaracao.detail?.bairro].filter(Boolean).join(', ')}</strong>, CEP <strong>{declaracao.detail?.cep||''}</strong>, {String(declaracao.detail?.cidade||'').toUpperCase()} - {String(declaracao.detail?.estado||'').toUpperCase()} RG nº <strong>{declaracao.detail?.rg||''}</strong>, declaro, sob as penas da lei, que não respondo a inquéritos policiais nem a processos criminais no estado de domicílio e nos entes federativos, e estou ciente de que, em caso de falsidade ideológica, ficarei sujeito às sanções prescritas no Código Penal e às demais cominações legais aplicáveis.</p><p className="art">Art. 299 - Omitir, em documento público ou particular, declaração que nele deveria constar, ou nele inserir ou fazer inserir declaração falsa ou diversa da que devia ser escrita, com o fim de prejudicar direito, criar obrigação ou alterar a verdade sobre o fato juridicamente relevante.</p><p>Pena - reclusão de 1 (um) a 5 (cinco) anos e multa, se o documento é público e reclusão de 1 (um) a 3 (três) anos, se o documento é particular.</p><p className="validade">Esta declaração tem validade até a data: {dataValidadeAtual()}</p><p className="local">Salvador- BA, {dataEmissaoAtual()}</p><div className="signatureBlock"><div className="signatureLine"></div><strong>{nomeCliente(declaracao.detail)}</strong><div className="cpf">CPF Nº {declaracao.detail?.cpf_cnpj||''}</div></div></>}
{declaracao.tipo==='titular'&&<><h1>DECLARAÇÃO DO TITULAR DO ENDEREÇO</h1><p>Eu <strong>{String(declaracao.titular?.nome||'').toUpperCase()}</strong>, RG nº {declaracao.titular?.rg||''}, CPF nº {declaracao.titular?.cpf||''}, declaro para os devidos fins que o senhor(a) <strong>{nomeCliente(declaracao.detail)}</strong> RG nº {declaracao.detail?.rg||''}, portador do CPF nº {declaracao.detail?.cpf_cnpj||''}, <strong>reside no endereço de minha propriedade</strong> localizado no (a) , {enderecoCliente(declaracao.detail)}, CEP {declaracao.detail?.cep||''}, {cidadeEstadoCliente(declaracao.detail)} e <strong>está autorizado a guardar seu acervo de PCE no respectivo endereço.</strong> <strong>Tenho ciência de que o local será usado para armazenamento de armas, munições e acessórios autorizados pelo Exército Brasileiro/Polícia Federal.</strong></p><p>Por ser verdade, dato e assino o presente documento, declarando estar ciente de que responderei criminalmente em caso de falsidade das informações aqui prestadas.</p><div className="signatureBlock titularSignature"><div className="signatureLine"></div><strong>{String(declaracao.titular?.nome||'').toUpperCase()}</strong><div>{declaracao.titular?.cpf||''}</div></div><p className="titularData">{dataEmissaoAtual()}</p></>}
{declaracao.tipo==='dsa'&&<><h1>DECLARAÇÃO DE SEGURANÇA DO ACERVO (DSA)</h1><p>Eu, <strong>{nomeCliente(declaracao.detail)}</strong>, nascido em {getNascimento(declaracao.detail)||'NÃO INFORMADO'}, filho(a) de {String(declaracao.detail?.filiacao_pai||'').toUpperCase()} e {String(declaracao.detail?.filiacao_mae||'').toUpperCase()} RG nº {declaracao.detail?.rg||''}{declaracao.detail?.rg_orgao ? ' - '+declaracao.detail.rg_orgao : ''}, BRASILEIRO, natural de {String(declaracao.detail?.naturalidade||'').toUpperCase()}, portador do CPF nº {declaracao.detail?.cpf_cnpj||''} residência no(a), {declaracao.detail?.endereco||''}, {declaracao.detail?.numero||''}, {declaracao.detail?.complemento||''}, {declaracao.detail?.bairro||''}, CEP {declaracao.detail?.cep||''}, {cidadeEstadoCliente(declaracao.detail)}.</p><p><strong>DECLARO</strong>, para fim de <strong>renovação</strong> de Certificado de Registro Arma de Fogo, na categoria pessoa física , que meu acervo/PCE será sempre mantido em local seguro.</p><p className="validade dsaValidade">Esta declaração tem validade até a data: <strong>{dataValidadeAtual()}</strong></p><p className="local dsaLocal">{cidadeEstadoCliente(declaracao.detail)}, {dataEmissaoAtual()}</p><div className="signatureBlock dsaAssinatura"><div className="signatureLine"></div><strong>{nomeCliente(declaracao.detail)}</strong><div>CPF Nº {declaracao.detail?.cpf_cnpj||''}</div></div></>}</div><div className="actions noPrint"><button type="button" className="secondary" onClick={()=>{setDeclaracao(null);setDetail(declaracao.detail)}}>Voltar</button><button type="button" onClick={()=>imprimirDeclaracao(declaracao)}>Imprimir / Salvar PDF</button></div></>}
</section></div>}{showUsuarios&&<div className="overlay"><section className="modal userModal"><div className="modalhead"><div><h2>Gestão de usuários</h2><p>Cadastre usuários, defina perfis e altere senhas.</p></div><button type="button" className="close" onClick={()=>setShowUsuarios(false)}>×</button></div><div className="userCreate"><h3>Novo usuário</h3><div className="grid"><label><span>E-mail *</span><input value={usuarioForm.email} onChange={e=>setUsuarioForm({...usuarioForm,email:e.target.value})}/></label><label><span>Senha inicial *</span><input type="password" value={usuarioForm.senha} onChange={e=>setUsuarioForm({...usuarioForm,senha:e.target.value})}/></label><label><span>Perfil</span><select value={usuarioForm.role} onChange={e=>setUsuarioForm({...usuarioForm,role:e.target.value})}><option value="operacional">Operacional</option><option value="consultor">Consultor</option><option value="gerente">Gerente</option><option value="administrador">Administrador</option></select></label></div><div className="actions"><button type="button" onClick={async()=>{setErro('');try{const r=await fetch(API+'/api/usuarios',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify(usuarioForm)});const d=await r.json();if(!r.ok)throw new Error(d.error);setUsuarioForm({email:'',senha:'',role:'operacional'});setOk('Usuário cadastrado com sucesso.');await loadUsuarios()}catch(e){setErro(e.message)}}}>Cadastrar usuário</button></div></div><div className="userList"><h3>Usuários cadastrados</h3>{usuarios.map(u=><div className="userRow" key={u.id}><div><b>{u.email}</b><small>{u.role} · {u.active?'Ativo':'Inativo'}</small></div><div className="userActions"><select value={u.role} onChange={async e=>{const role=e.target.value;await fetch(API+'/api/usuarios/'+u.id,{method:'PUT',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify({role,active:u.active})});await loadUsuarios()}}><option value="operacional">Operacional</option><option value="consultor">Consultor</option><option value="gerente">Gerente</option><option value="administrador">Administrador</option></select><button type="button" className="secondary" onClick={()=>{setSenhaUsuarioId(u.id);setSenhaForm({atual:'',nova:''})}}>Alterar senha</button><button type="button" className="secondary" onClick={async()=>{await fetch(API+'/api/usuarios/'+u.id,{method:'PUT',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify({role:u.role,active:!u.active})});await loadUsuarios()}}>{u.active?'Desativar':'Ativar'}</button></div>{senhaUsuarioId===u.id&&<div className="passwordRow"><input type="password" placeholder="Nova senha (mín. 6)" value={senhaForm.nova} onChange={e=>setSenhaForm({atual:'',nova:e.target.value})}/><button type="button" onClick={async()=>{try{const r=await fetch(API+'/api/usuarios/'+u.id+'/senha',{method:'PUT',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token},body:JSON.stringify({senha:senhaForm.nova})});const d=await r.json();if(!r.ok)throw new Error(d.error);setSenhaUsuarioId(null);setOk('Senha alterada com sucesso.')}catch(e){setErro(e.message)}}}>Salvar senha</button></div>}</div>)}</div>{erro&&<div className="msg error">{erro}</div>}{ok&&<div className="msg success">{ok}</div>}<div className="actions"><button type="button" className="secondary" onClick={()=>setShowUsuarios(false)}>Fechar</button></div></section></div>}{show&&<div className="overlay"><section className="modal"><div className="modalhead"><div><h2>{editing?'Editar cliente':'Novo cliente'}</h2><p>Cadastro completo do cliente</p></div><button className="close" onClick={()=>{setShow(false);setEditing(false)}}>×</button></div><form onSubmit={save}>
<h3>1. Identificação</h3><div className="grid"><F label="Código" name="codigo" form={form} setForm={setForm}/><F label="Nome completo / Razão social" name="nome_completo" required form={form} setForm={setForm}/><F label="CPF/CNPJ" name="cpf_cnpj" required form={form} setForm={setForm}/><F label="Data de nascimento / fundação" name="nascimento" type="date" form={form} setForm={setForm}/><F label="Naturalidade" name="naturalidade" form={form} setForm={setForm}/><F label="Filiação - Pai" name="filiacao_pai" form={form} setForm={setForm}/><F label="Filiação - Mãe" name="filiacao_mae" form={form} setForm={setForm}/><F label="Estado civil" name="estado_civil" form={form} setForm={setForm}/><F label="RG" name="rg" form={form} setForm={setForm}/><F label="Órgão RG" name="rg_orgao" form={form} setForm={setForm}/><label><span>Tipo de cliente</span><select value={form.tipo_cliente} onChange={e=>setForm({...form,tipo_cliente:e.target.value})}><option>Pessoa Física</option><option>Pessoa Jurídica</option></select></label><label><span>Status</span><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option value="ativo">Ativo</option><option value="inativo">Inativo</option><option value="bloqueado">Bloqueado</option></select></label></div>
<h3>2. Contatos</h3><div className="grid"><F label="Telefone" name="telefone" form={form} setForm={setForm}/><F label="WhatsApp" name="whatsapp" form={form} setForm={setForm}/><F label="E-mail" name="email" form={form} setForm={setForm}/></div>
<h3>3. Endereço</h3><div className="grid"><F label="CEP" name="cep" form={form} setForm={setForm}/><F label="Endereço" name="endereco" form={form} setForm={setForm}/><F label="Número" name="numero" form={form} setForm={setForm}/><F label="Complemento" name="complemento" form={form} setForm={setForm}/><F label="Bairro" name="bairro" form={form} setForm={setForm}/><F label="Cidade" name="cidade" form={form} setForm={setForm}/><F label="Estado" name="estado" form={form} setForm={setForm}/></div>
<h3>4. Gestão do cadastro</h3><div className="grid"><F label="Origem do cadastro" name="origem_cadastro" form={form} setForm={setForm}/><F label="Responsável" name="responsavel" form={form} setForm={setForm}/></div>
<h3>5. Perfil CAC</h3><div className="grid"><F label="Categoria CAC" name="categoria_cac" form={form} setForm={setForm}/><F label="Número do CR" name="cr_numero" form={form} setForm={setForm}/><F label="Emissão do CR" name="cr_emissao" type="date" form={form} setForm={setForm}/><F label="Validade do CR" name="cr_validade" type="date" form={form} setForm={setForm}/><label><span>Situação do CR</span><select value={form.cr_situacao} onChange={e=>setForm({...form,cr_situacao:e.target.value})}><option value="">Selecione</option><option>Regular</option><option>Próximo do vencimento</option><option>Vencido</option><option>Em análise</option></select></label><F label="Órgão emissor" name="cr_orgao" form={form} setForm={setForm}/></div>
<h3>6. Observações</h3><textarea rows="4" value={form.observacoes} onChange={e=>setForm({...form,observacoes:e.target.value})} placeholder="Informações relevantes sobre o cliente..."/>
{erro&&<div className="msg error">{erro}</div>}{ok&&<div className="msg success">{ok}</div>}<div className="actions"><button type="button" className="secondary" onClick={()=>{setShow(false);setEditing(false)}}>Cancelar</button><button type="submit">{editing?'Salvar alterações':'Salvar cliente'}</button></div></form></section></div>}</main>}
createRoot(document.getElementById('root')).render(<App/>);